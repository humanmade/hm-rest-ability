name: Release

# Stamps the version into the plugin header, then creates an immutable tag
# that already points at the versioned code.
#
# The tag is created exactly once and is never moved or force-pushed, so
# Packagist (which requires tags to be immutable) accepts it without
# conflict.
#
# Trigger this manually from the Actions tab and supply the version to cut.

on:
    workflow_dispatch:
        inputs:
            version:
                description: 'Version to release, without a leading "v" (e.g. 0.2.0)'
                required: true
                type: string

concurrency:
    group: release-${{ github.event.inputs.version }}
    cancel-in-progress: false

jobs:
    release:
        name: Tag and publish
        runs-on: ubuntu-latest
        permissions:
            contents: write
        steps:
            - name: Validate version input
              run: |
                  VERSION="${{ github.event.inputs.version }}"
                  if ! printf '%s' "$VERSION" | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+$'; then
                      echo "::error::Version must be in X.Y.Z format without a leading 'v'. Got: '$VERSION'"
                      exit 1
                  fi
                  echo "VERSION=$VERSION" >> "$GITHUB_ENV"
                  echo "TAG=v$VERSION" >> "$GITHUB_ENV"

            - name: Check out source
              uses: actions/checkout@v5
              with:
                  ref: main
                  fetch-depth: 0

            - name: Ensure the tag does not already exist
              run: |
                  if git ls-remote --tags origin | grep -q "refs/tags/${TAG}$"; then
                      echo "::error::Tag ${TAG} already exists. Tags are immutable — bump the version and try again."
                      exit 1
                  fi

            - name: Stamp version into the plugin header
              run: |
                  sed -i "s/__VERSION__/${VERSION}/g" hm-rest-ability.php
                  echo "Stamped plugin header / constant:"
                  grep -n "Version:\|HM_REST_ABILITY_VERSION" hm-rest-ability.php

            - name: Create the immutable release commit and tag
              run: |
                  git config user.name "github-actions[bot]"
                  git config user.email "github-actions[bot]@users.noreply.github.com"

                  git add hm-rest-ability.php
                  git commit -m "Release ${TAG}"

                  git tag -a "${TAG}" -m "Release ${TAG}"

                  # Push ONLY the tag (and the objects it reaches). It is created
                  # once and never force-pushed, satisfying Packagist's immutability.
                  git push origin "refs/tags/${TAG}"

            - name: Build distribution ZIP
              run: |
                  mkdir -p dist
                  # git archive honours the export-ignore rules in .gitattributes,
                  # so the ZIP contents are defined in exactly one place and are
                  # guaranteed to match the tagged tree.
                  git archive --format=zip \
                      --prefix=hm-rest-ability/ \
                      -o "dist/hm-rest-ability-${TAG}.zip" \
                      "${TAG}"
                  echo "Created dist/hm-rest-ability-${TAG}.zip"

            - name: Publish GitHub release
              uses: softprops/action-gh-release@v3
              with:
                  tag_name: ${{ env.TAG }}
                  name: ${{ env.TAG }}
                  generate_release_notes: true
                  files: dist/hm-rest-ability-${{ env.TAG }}.zip
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
